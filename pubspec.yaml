name: nasa_flutter
description: "NASA's Astronomy Picture of the Day - Flutter version."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.5
  get_it: ^7.7.0
  dio: ^5.4.3+1
  intl: ^0.19.0
  hive: ^2.2.3
  hive_flutter: # Version from hive

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
